# SPEC.md — CSV加工Webアプリ 仕様書

## 1. 概要

### 1.1 目的
個人利用のCSV加工Webアプリ。CSVファイルをドラッグ＆ドロップまたはファイル選択で読み込み、列の選択・並び順変更・リネームと、値の変換（trim／正規表現置換）を施して新しいCSVファイルとして出力する。

### 1.2 利用シーン
- 同じ形式のCSV（月次レポート、業務データ等）を**繰り返し**処理するケース
  - テンプレート機能で設定を保存・再利用
- 都度違うCSVに対して**その場限り**で列を選び・整形するケース
  - テンプレートを使わず即時操作

### 1.3 設計の核となる前提
- **完全クライアントサイド**：機微情報を含むCSVも想定するため、ファイルはサーバーに送らない。
- **大容量対応**：100万行超／100MB以上のCSVも扱える。
- **個人利用**：認証・複数ユーザー・チーム共有は対象外。

---

## 2. 機能要件

### 2.1 MVP（フェーズ1）

#### 2.1.1 ファイル入力
- ドラッグ＆ドロップでCSV投入
- ファイル選択ダイアログでも投入可能
- 1ファイル単位の処理

#### 2.1.2 エンコーディング対応
- 入力：UTF-8（BOM有無）／Shift_JIS／CP932
- 自動判定し、UI上で手動切替も可能
- 出力：**入力と同じエンコーディングを維持**（BOMの有無も継承）

#### 2.1.3 ヘッダ
- 1行ヘッダのみ（先頭行をヘッダとみなす）
- 拡張で複数行ヘッダ・先頭注釈行スキップに対応

#### 2.1.4 列操作
- **列の選択／除外**：チェックボックスで出力対象列を切替
- **列のリネーム**：出力ヘッダ名を編集
- **列の並び順変更**：DnD（@dnd-kit/sortable）で順序を入れ替え

#### 2.1.5 値の変換
1列に対して**複数の変換を順番付きで適用**するパイプライン構造。

- **trim プリセット**：前後空白（半角・全角・タブ）を除去
- **正規表現置換**：パターン＋置換文字列＋フラグ（`g`／`i` のチェックボックス指定）

未対応のプリセット（拡張対象、または対応外）：
- 全角↔半角変換、大文字↔小文字、日付フォーマット、列の分割・結合、JS式評価

#### 2.1.6 プレビュー
- ビフォア／アフター並列表示
- 先頭100行
- ルール変更時はメインスレッドで即時アフター再変換（Worker往復なし）

#### 2.1.7 実行と出力
- 「実行」ボタンでフル処理を Web Worker で起動
- 進捗バー（処理済みバイト／全体バイト）
- キャンセルボタンで協調的中断
- 完了後、ダウンロードリンクが有効化
- 出力ファイル名：`<元のファイル名>_processed.csv`（自動）

#### 2.1.8 エラーハンドリング
- **エラー行＝列数がヘッダと一致しない行**
- エラー行は元の値のまま出力（スキップしない）
- 完了時にエラーサマリ（件数と最初のN件の行番号・該当列）を表示
- 値変換中の例外は MVP ではエラーサマリに含めない（`try/catch` でログ程度）

#### 2.1.9 テンプレート機能
- IndexedDB（Dexie）に保存
- ID で管理。**名前重複は許可**し、保存時に警告表示
- 機能：保存・一覧・読込・削除
- 列名の**厳密一致**でテンプレートと現在のCSVを照合
  - 「厳密一致」＝大文字小文字・前後空白・全角半角・ゼロ幅スペースを区別する完全一致
  - 未マッチ列は警告を出してそのルールをスキップ。マッチした列のみ処理

#### 2.1.10 配置・デプロイ
- GitHub Pages 等の静的ホスティング
- GitHub Actions による自動デプロイ
- ローカルでも `npm run dev` で動作

### 2.2 拡張（フェーズ2）

#### 2.2.1 複数ファイル一括処理
- 複数ファイルをD&D／選択
- 同一テンプレートを全ファイルに適用
- 出力は ZIP（fflate ストリーミング）で一括ダウンロード
- ファイル単位の進捗＋全体進捗を表示
- ファイルごとに列名スキーマが違ってもスキップ／警告で続行

#### 2.2.2 テンプレートのエクスポート／インポート
- JSON 形式（スキーマバージョン付き）
- 1ファイル＝1テンプレートのシンプル形式
- インポート時はバージョン互換チェック

#### 2.2.3 複数行ヘッダ・スキップ行
- 「先頭M行をスキップ」「N行目をヘッダ」を手動指定
- 複数行ヘッダの結合方式は `/` 区切り（`上段/下段`）

#### 2.2.4 自動ヘッダ検出の高度化
- 列数の安定性で本ヘッダ行を推定
- 注釈行（`#` 始まり、空白多数等）の自動スキップ提案
- 提案を UI で確認・修正可能

#### 2.2.5 正規表現フラグ拡充
- MVP の `g`／`i` に加え `m`／`s`／`u` を追加

#### 2.2.6 パフォーマンスチューニング
- チャンクサイズ調整
- Transferable Objects による Worker→メインのコピー回避
- 行オブジェクトのメモリプロファイリング

---

## 3. 非機能要件

### 3.1 パフォーマンス
- 100万行／100MB のCSVをブラウザでフリーズなく処理できる
- 進捗が更新され、ユーザーがいつでもキャンセル可能
- プレビュー（先頭100行）は即時（1秒未満）

### 3.2 セキュリティ／プライバシー
- ファイルは一切ネットワーク送信しない（fetch／XHR／WebSocket での外部送信を行わない）
- 永続化対象は IndexedDB のテンプレート設定のみ。CSV本体は永続化しない
- 第三者ライブラリの追加は最小限に絞り、依存ツリーを定期的に確認

### 3.3 ブラウザ要件
- 最新の Chrome／Edge を主対象
- File System Access API には依存しない（古いブラウザでも動くよう `<input type=file>` ベース）
- IndexedDB／Web Worker／Fetch API は必須

### 3.4 アクセシビリティ
- 個人利用のため厳密な WCAG 準拠は対象外
- ただしキーボード操作で主要操作（ファイル選択、ルール追加・削除、実行）が可能であること

### 3.5 国際化
- UI は日本語
- 入力データは多言語可（エンコーディングの範囲内で）

---

## 4. CSV方言サポート

### 4.1 エンコーディング
| エンコーディング | 入力 | 出力 |
|---|---|---|
| UTF-8（BOMなし） | ◯ | ◯（入力と同じ場合）|
| UTF-8（BOM付き） | ◯ | ◯（入力と同じ場合）|
| Shift_JIS | ◯ | ◯（入力と同じ場合）|
| CP932 | ◯ | ◯（入力と同じ場合）|
| その他（EUC-JP、UTF-16等） | × | × |

### 4.2 区切り文字
- カンマ（`,`）のみ
- タブ・セミコロン等は対象外

### 4.3 引用符
- ダブルクォート（`"`）対応
- クォート内のカンマ・改行・エスケープ（`""`）に対応（PapaParse に委任）

### 4.4 改行コード
- 入力：CRLF／LF どちらも受け入れる
- 出力：CRLF（Excel互換）

### 4.5 ヘッダ
- MVP：1行固定
- 拡張：複数行ヘッダ・先頭注釈行スキップ

---

## 5. データフロー

### 5.1 全体図

```
[File] 
  ↓ File.stream()
[Uint8Array チャンク] 
  ↓ デコーダ（UTF-8 or SJIS、stream: true）
[string チャンク] 
  ↓ PapaParse.parse(chunk, { header: true })
[行オブジェクト配列] 
  ↓ 変換パイプライン適用（列選択 → リネーム → trim/regex → 並び順）
[変換後行配列] 
  ↓ CSV シリアライズ
[string チャンク] 
  ↓ エンコーダ
[Uint8Array チャンク] 
  ↓ Blob 蓄積
[出力 Blob] 
  ↓ URL.createObjectURL → <a download>
[ダウンロード]
```

### 5.2 メイン↔Worker

```
メインスレッド                     Web Worker
───────────────────                 ─────────────────
 ファイル受領
 先頭2MB 読込・100行デコード
 (ビフォアキャッシュ)
                                    
 ルール変更 → キャッシュへ即適用
 (アフター)
                                    
 「実行」ボタン押下 ───────────────→ ファイル全体処理開始
                          ←──────── 進捗 (100msスロットリング)
 進捗バー更新
                          ←──────── エラーサマリ
                          ←──────── 完了 + Blob
 ダウンロード起動
 
 「キャンセル」───────→ AbortController.abort()
                                    signal.aborted を検知
                                    parser.abort() / reader.cancel()
                                    リソース解放（try/finally）
                          ←──────── cancelled 通知
```

### 5.3 プレビュー戦略
- ファイル読込直後に先頭2MBを読み、PapaParse で100行デコード → メインに保持
- ルール編集時、この100行に対してメインスレッドで変換適用 → アフター描画
- 実行時のみ Worker でフル処理。プレビューはフル処理結果に置き換えない

---

## 6. UI/UX

### 6.1 1画面レイアウト

```
┌──────────────────────────────────────────────────────────────────┐
│ [ヘッダ]  CSV加工アプリ   テンプレート: [選択▼] [保存] [実行] [キャンセル] │
├────────────────────────┬─────────────────────────────────────────┤
│ [左サイド: 設定]        │ [右上: ビフォアプレビュー]              │
│ ・エンコーディング      │  | 列A | 列B | 列C |                    │
│ ・ヘッダ設定           │  | aaa | 1   | xx  |                    │
│ ・列選択／リネーム／    │  | bbb | 2   | yy  |                    │
│   並び順変更（DnD）    │ ...先頭100行                            │
│ ・ルールエディタ        │                                         │
│  - 列ごとの変換         │ [右下: アフタープレビュー]              │
│   パイプライン         │  | 列A' | 列C |                         │
│   ・trim                │  | aaa  | xx  |                         │
│   ・regex 置換         │  | bbb  | yy  |                         │
│   （順番付き）         │ ...先頭100行                            │
│                        │                                         │
│ ・テンプレート一覧      │                                         │
└────────────────────────┴─────────────────────────────────────────┘
```

### 6.2 主要画面遷移

1. 初期画面：中央にドロップゾーン
2. ファイル投入 → エンコーディング自動判定 → 設定画面に遷移
3. 設定編集 → リアルタイムでアフタープレビュー更新
4. 「実行」 → モーダルで進捗バー＋キャンセル
5. 完了 → ダウンロードボタン表示／エラーサマリ表示
6. テンプレート保存／読込／削除はサイドメニューから

---

## 7. テンプレート仕様

### 7.1 JSON スキーマ（v1）

```json
{
  "schemaVersion": 1,
  "id": "<uuid>",
  "name": "テンプレート名",
  "createdAt": "2026-05-09T10:00:00.000Z",
  "updatedAt": "2026-05-09T10:00:00.000Z",
  "header": {
    "mode": "single"
  },
  "outputColumns": [
    {
      "sourceName": "氏名",
      "outputName": "name",
      "order": 0,
      "rules": [
        { "type": "trim" },
        {
          "type": "regexReplace",
          "pattern": "\\s+",
          "replacement": " ",
          "flags": ["g"]
        }
      ]
    }
  ],
  "outputEncoding": "preserve"
}
```

### 7.2 列名照合ルール
- `outputColumns[].sourceName` を**厳密一致**で対象CSVの列名と照合
  - 大文字小文字・前後空白・全角半角・ゼロ幅スペースをすべて区別
- マッチしない列のルールはスキップし、UIに警告表示
- マッチした列のみ「選択／リネーム／並び順／変換」を適用

### 7.3 IndexedDB スキーマ（Dexie）

```ts
db.version(1).stores({
  templates: '&id, name, createdAt, updatedAt'
});

interface Template {
  schemaVersion: 1;
  id: string;
  name: string;
  createdAt: string;
  updatedAt: string;
  header: { mode: 'single' };
  outputColumns: OutputColumn[];
  outputEncoding: 'preserve';
}
```

スキーマ変更時は `db.version(N+1).stores(...).upgrade(...)` で必ずマイグレーション関数を提供する。

### 7.4 名前重複ポリシー
- ID で一意管理。**名前重複は許可**
- 保存ダイアログで「同名のテンプレートが既に存在します」と警告表示

### 7.5 拡張：エクスポート／インポート
- JSON ファイル（拡張子 `.json` または `.csvtpl.json`）
- スキーマバージョンが古い場合は移行関数（`migrateV1ToV2` 等）を経由
- インポート時、ID が衝突したら新IDを採番して別エントリとして保存

---

## 8. エラーハンドリング

### 8.1 エラー行の定義
- ヘッダの列数と一致しない行を「エラー行」とする
- エラー行は**元の値のまま出力**（スキップしない）
- 列数不足：足りない列は空文字で埋める／列数超過：余剰列は無視

### 8.2 エラーサマリ
- 完了後、UIに以下を表示
  - エラー行の総数
  - 最初の N 件（既定10件）の行番号
  - サンプルとして元の行データ
- サマリはダウンロードファイルとは分離して画面に表示する

### 8.3 値変換時の例外
- 不正な正規表現は登録時にバリデーション（`new RegExp` を try で試行）
- 実行中に予期せぬ例外が出た場合は元の値を保持し、コンソールにログ
- MVP ではエラーサマリに含めない

### 8.4 キャンセル時
- 部分結果は破棄
- ダウンロードリンクは無効
- 現在状態は「キャンセル済み」として表示

### 8.5 致命的エラー
- ファイル読込失敗、エンコーディング判定不能など：
  - エラーダイアログ表示
  - 操作を初期状態に戻す（ファイル投入待ちへ）

---

## 9. 制約事項・対応外

### 9.1 機能スコープ外（実装しない）
- 行のフィルタリング・並び替え・重複除去
- 列の分割・結合
- trim 以外の値変換プリセット（全角↔半角、大文字↔小文字、日付フォーマット等）
- JS 式の評価
- カンマ以外の区切り文字（タブ・セミコロン）
- 出力エンコーディングのユーザー選択（常に入力と同じ）
- サーバーサイド処理・認証・複数ユーザー対応
- モバイル端末向けレイアウト最適化

### 9.2 技術的制約
- ファイルサイズの上限はブラウザのメモリ／File API 仕様に依存（明示的な制限は設けない）
- IndexedDB のストレージ上限はブラウザに依存
- Safari ITP により IndexedDB が定期的にクリアされる可能性あり
- ReDoS（正規表現の暴走）：MVP ではタイムアウト機構を入れない（Worker 終了で対処）

### 9.3 将来の検討事項
- PWA 化（オフライン動作）
- ダークモード
- テンプレートのタグ／検索
- 直近ファイル履歴
- E2E テスト整備

---

## 10. 用語集

| 用語 | 意味 |
|---|---|
| MVP | 最小実装範囲。フェーズ1で実装する機能群 |
| 拡張 | フェーズ2で追加する機能群 |
| ビフォア／アフター | 変換前／変換後のCSV状態 |
| パイプライン | 1列に対する変換を順番付きで連鎖させた構造 |
| 厳密一致 | 大文字小文字・前後空白・全角半角を区別する完全文字列一致 |
| エラー行 | ヘッダの列数と一致しない行 |
| テンプレート | 列選択・リネーム・並び順・変換ルール一式に名前を付けて保存したもの |
